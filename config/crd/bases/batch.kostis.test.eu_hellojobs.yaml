---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: hellojobs.batch.kostis.test.eu
spec:
  group: batch.kostis.test.eu
  names:
    kind: HelloJob
    listKind: HelloJobList
    plural: hellojobs
    singular: hellojob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: HelloJob is the Schema for the hellojobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HelloJobSpec defines the desired state of HelloJob
            properties:
              delayseconds:
                type: integer
              image:
                description: Foo is an example field of HelloJob. Edit hellojob_types.go
                  to remove/update
                type: string
              message:
                type: string
            required:
            - image
            type: object
          status:
            description: HelloJobStatus defines the observed state of HelloJob
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
